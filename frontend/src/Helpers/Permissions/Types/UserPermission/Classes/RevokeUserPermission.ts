import { UserPermission } from './UserPermission';
import { IRevokeUserPermission } from '../Interfaces/IRevokeUserPermission';

export class RevokeUserPermission extends UserPermission implements IRevokeUserPermission {
    
}