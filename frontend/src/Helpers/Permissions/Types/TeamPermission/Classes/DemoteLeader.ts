import { TeamPermission } from './TeamPermission';
import { IDemoteLeader } from '../Interfaces/IDemoteLeader';

export class DemoteLeader extends TeamPermission implements IDemoteLeader {
    
}