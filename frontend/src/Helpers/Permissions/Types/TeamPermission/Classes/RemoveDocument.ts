import { TeamPermission } from './TeamPermission';
import { IRemoveDocument } from '../Interfaces/IRemoveDocument';

export class RemoveDocument extends TeamPermission implements IRemoveDocument {
    
}