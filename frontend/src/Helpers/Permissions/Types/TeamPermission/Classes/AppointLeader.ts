import { TeamPermission } from './TeamPermission';
import { IAppointLeader } from '../Interfaces/IAppointLeader';

export class AppointLeader extends TeamPermission implements IAppointLeader {
    
}