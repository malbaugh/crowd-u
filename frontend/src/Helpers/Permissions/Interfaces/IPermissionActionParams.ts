
export interface IPermissionActionParams {
    // Anything that inherits from this can be passed into a permission action object
    // This means that data that is passed into a permission action object is defined at runtime
    // A permission action's PerformAction method is also defined at runtime which uses the permission action params object

    
}